#include<bits/stdc++.h>
using namespace std;
// https://wiki.vnoi.info/algo/string/trie
#define task "taptanh"
const int N = 1e4 + 9;
int n, child[N * 50][26];
string s[N];
int exist[N * 50], cnt = 0;
vector<string> res;
void Add(string &x){
    int k = 0;
    for (int i = 0; i < x.size(); i++){
        int  c = x[i] - 'a';
        if (child[k][c] == 0){cnt++; child[k][c] = cnt;}
        k = child[k][c];
    }
    exist[k]++;
}
void dfs(int pos, vector<string> &res, string &current){
    for(int i = 1; i <= exist[pos]; i++) res.push_back(current);
    for (int i = 0; i < 26; i++) if (child[pos][i] != 0){
        current = current + char(i + 'a');
        dfs(child[pos][i], res, current);
        current.pop_back();
    }
}
void solve(){
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> s[i];
    for (int i = 1; i <= n; i++) Add(s[i]);
    string current = "";
    dfs(0, res, current);
    for(string it : res) cout << it << '\n';
}
signed main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);
    if(fopen(task".inp","r")){
        freopen(task".inp","r",stdin);
        freopen(task".out","w",stdout);
    }
    solve();
    return 0;
}
